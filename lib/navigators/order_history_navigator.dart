import 'package:flutter/material.dart';

class OrderHistoryNavigator extends StatelessWidget {
  const OrderHistoryNavigator({super.key});

  @override
  Widget build(BuildContext context) {
    return Navigator(
      onGenerateRoute: (RouteSettings settings) {
        // Define routes for order history
      },
    );
  }
}
